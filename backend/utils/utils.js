const prompt = "";

const currency = "usd";
const paymentMethods = ["stripe", "razorpay", "manual"];
const freeItemRewriteCount = 1;

const merchantName = "BotStudio";
const merchantAddress = "BotStudio"
const razorpayThemeColor = "#528ff0";

export { merchantName, merchantAddress, prompt, currency, paymentMethods, freeItemRewriteCount, razorpayThemeColor, logoBase64 };